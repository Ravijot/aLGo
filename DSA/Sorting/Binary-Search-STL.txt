#include algorithm 
#include iostream 
using namespace std; 
void show(int a[], int arraysize) 
{ 
    for (int i = 0; i  arraysize; ++i) 
    {
        cout  a[i]   ;
	}
} 
int main() 
{ 
    int a[] = { 1, 5, 8, 9, 6, 7, 3, 4, 2, 0 }; 
    int asize = sizeof(a)  sizeof(a[0]); 
    cout  The array is  ; 
    show(a, asize); 
    Let's say we want to search for 2 in the array
    So, we first sort the array
    sort(a, a + asize); 
    cout  nThe array after sorting is  ; 
    show(a, asize); 
    Now, we do the binary search
    if (binary_search(a, a + asize, 6)) 
        cout  nElement found in the array; 
    else
        cout  nElement not found in the array; 
    To find index of Element
    auto index=lower_bound(a,a+asize,6);
    coutnIndex of 6 is (index-a);
    return 0; 
} 