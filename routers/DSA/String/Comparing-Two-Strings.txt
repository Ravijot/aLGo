#include<iostream>
#include<conio.h>
using namespace std;
int compare(char a[],char b[])
{
	int i=0;
	while( a[i]!='\0' && b[i]!='\0')
	{
		if(a[i]!=b[i])
		{
			return -1;
		}
		i++;
	}
	return 0;
}
int main()
{
  char a[30],b[30];
  cout<<"Enter first string\n";
  cin.getline(a,30);
  cout<<"\nEnter the string\n";
  cin.getline(b,30);
  int x;
  x=compare(a,b);
  	if(x==0)
  	{
  		cout<<"\nStrings are equal";
	  }
	  else
	  {
	  	cout<<"\nStrings are not equal";
	  }
	  getch();
	  return 0;
}